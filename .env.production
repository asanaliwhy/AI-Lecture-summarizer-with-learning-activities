# ══════════════════════════════════════════════
# Lectura — Production Environment Configuration
# ══════════════════════════════════════════════
# Copy this file to backend/.env and fill in real values

# ─── Server ───
PORT=8081
ENV=production

# ─── PostgreSQL ───
DATABASE_URL=postgres://postgres:CHANGE_ME_STRONG_PASSWORD@postgres:5432/lectura?sslmode=disable

# ─── Redis ───
REDIS_URL=redis://redis:6379/0

# ─── JWT ───
# Generate with: openssl rand -hex 64
JWT_SECRET=CHANGE_ME_GENERATE_WITH_OPENSSL_RAND_HEX_64

# ─── Google Gemini AI ───
# Get from: https://aistudio.google.com/apikey
GEMINI_API_KEY=CHANGE_ME_YOUR_GEMINI_API_KEY
GEMINI_REQUESTS_PER_MINUTE=30
GEMINI_TOKENS_PER_MINUTE=500000
GEMINI_CONCURRENT_REQUESTS=3

# ─── Storage ───
STORAGE_TYPE=local
STORAGE_PATH=./uploads

# ─── SMTP (Email) ───
# Gmail: enable 2FA → create App Password at https://myaccount.google.com/apppasswords
# Or use services like SendGrid, Mailgun, etc.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-16-char-app-password
SMTP_FROM=noreply@lectura.app

# ─── Frontend URL ───
FRONTEND_URL=https://your-domain.com

# ─── Docker Compose Overrides ───
POSTGRES_USER=postgres
POSTGRES_PASSWORD=CHANGE_ME_STRONG_PASSWORD
POSTGRES_DB=lectura
POSTGRES_PORT=5432
REDIS_PORT=6379
BACKEND_PORT=8081
FRONTEND_PORT=3000
